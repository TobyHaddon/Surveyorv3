<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Surveyor.User_Controls.Compare"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Surveyor.User_Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid MinWidth="700" MaxWidth="700" MinHeight="600">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="140"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="450"/>
            </Grid.RowDefinitions>
            
            <!-- Select calibration project to compare -->
            <Button x:Name="SelectProject1" Grid.Row="0" Grid.Column="0" Content="Compare Against..." Click="SelectProject1_Click" IsEnabled="True" Margin="0,10,0,0"/>
            <TextBlock x:Name="Project1Name" Grid.Row="0" Grid.Column="1" VerticalAlignment="Center" Margin="10,10,0,0"/>
            <Button x:Name="SelectProject2" Grid.Row="1" Grid.Column="0" Content="Compare To..." Click="SelectProject2_Click" IsEnabled="True" Margin="0,10,0,0"/>
            <TextBlock x:Name="Project2Name" Grid.Row="1" Grid.Column="1" VerticalAlignment="Center" Margin="10,10,0,0"/>
            <Button x:Name="SelectProject3" Grid.Row="2" Grid.Column="0" Content="Compare To..." Click="SelectProject3_Click" IsEnabled="True" Margin="0,10,0,0"/>
            <TextBlock x:Name="Project3Name" Grid.Row="2" Grid.Column="1"  VerticalAlignment="Center"  Margin="10,10,0,0"/>

            <!-- Results -->
            <Border x:Name="ResultsBorder" Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" 
                    HorizontalAlignment="Stretch" VerticalAlignment="Stretch" BorderBrush="Gray" 
                    BorderThickness="1" Padding="4" CornerRadius="2" Margin="0,10,0,0">
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <TextBlock x:Name="Results"
                               FontFamily="Consolas"
                               FontSize="10"
                               TextWrapping="Wrap"
                               TextTrimming="None"
                               xml:space="preserve" />
                </ScrollViewer>
            </Border>

        </Grid>
    </Grid>
</UserControl>
