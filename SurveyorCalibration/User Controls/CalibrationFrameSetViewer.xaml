<?xml version="1.0" encoding="utf-8"?>
    <UserControl
    x:Class="Surveyor.Controls.CalibrationFrameSetViewer"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Surveyor.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="800"
    d:DesignWidth="600">

    <UserControl.Content>
        <Grid>
             
            <ScrollViewer HorizontalScrollBarVisibility="Visible">
                <Grid x:Name="MainPanel" Padding="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Box 1: Line graphs -->
                    <Border Grid.Column="0" Grid.Row="0" Grid.RowSpan="2" BorderBrush="Gray" BorderThickness="1">
                        <Grid Width="300">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Canvas x:Name="MovementCanvas" Grid.Row="0"/>
                            <Canvas x:Name="BlurCanvas" Grid.Row="1"/>
                        </Grid>
                    </Border>
                    
                    <!-- Sensor Coverage Bins -->
                    <TextBlock Text="Sensor Coverage" Grid.Column="1" Grid.Row="0" Padding="0" VerticalAlignment="Bottom" FontFamily="Segoe UI Variable" FontSize="10" HorizontalAlignment="Left" Margin="6,0,0,0"/>
                    <GridView x:Name="SensorBinGridItemsControl" Grid.Column="1" Grid.Row="1" VerticalAlignment="Top" HorizontalAlignment="Left" IsItemClickEnabled="False" SelectionMode="None" Padding="0" Margin="0">
                        <GridView.ItemsPanel>
                            <ItemsPanelTemplate>
                                <ItemsWrapGrid Orientation="Vertical" MaximumRowsOrColumns="1"/>
                            </ItemsPanelTemplate>
                        </GridView.ItemsPanel>
                        
                        <GridView.ItemContainerStyle>
                            <Style TargetType="GridViewItem">
                                <Setter Property="Padding" Value="0"/>
                                <Setter Property="Padding" Value="0"/>
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="VerticalContentAlignment" Value="Stretch"/>
                            </Style>
                        </GridView.ItemContainerStyle>
                    </GridView>

                    <!-- Pose Bins -->
                    <TextBlock Text="Pose Diversity" Grid.Column="2" Grid.Row="0" VerticalAlignment="Bottom" FontFamily="Segoe UI Variable" FontSize="10" HorizontalAlignment="Left" Margin="6,0,0,0"/>
                    <GridView x:Name="PoseBinGridItemsControl" Grid.Column="2" Grid.Row="1" VerticalAlignment="Top" HorizontalAlignment="Left" IsItemClickEnabled="False" SelectionMode="None">
                        <GridView.ItemsPanel>
                            <ItemsPanelTemplate>
                                <ItemsWrapGrid Orientation="Vertical" MaximumRowsOrColumns="1"/>
                            </ItemsPanelTemplate>
                        </GridView.ItemsPanel>

                        <GridView.ItemContainerStyle>
                            <Style TargetType="GridViewItem">
                                <Setter Property="Padding" Value="0"/>
                                <Setter Property="Padding" Value="0"/>
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Setter Property="VerticalContentAlignment" Value="Stretch"/>
                            </Style>
                        </GridView.ItemContainerStyle>
                    </GridView>

                </Grid>
            </ScrollViewer>
        </Grid>
    </UserControl.Content>
</UserControl>

