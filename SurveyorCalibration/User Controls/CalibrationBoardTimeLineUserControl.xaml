<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Surveyor.Controls.CalibrationBoardTimeLineUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Surveyor"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Viewbox Stretch="Fill" StretchDirection="DownOnly" Height="5" SizeChanged="Viewbox_SizeChanged">
            <Border BorderThickness="2" BorderBrush="Gray">
                <Canvas x:Name="CalibrationBoardTimeLine" Width="100" Height="5" Background="IndianRed"
                        ToolTipService.ToolTip="Red indicates outside of the zone where a calibration board has been detected. Green means a board has been detected. Black means we are inside the board detection zone but no board detected at this particular point (i.e. camera pointing elsewhere)"
                        PointerPressed="CalibrationBoardTimeLine_PointerPressed"/>
            </Border>
        </Viewbox>
    </Grid>
</UserControl>
