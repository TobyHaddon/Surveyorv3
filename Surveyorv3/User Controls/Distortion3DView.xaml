<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Surveyor.User_Controls.Distortion3DView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Surveyor.User_Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>


        <!-- Toolbar with some toggles -->
        <StackPanel Orientation="Horizontal" Spacing="12" Margin="12">
            <TextBlock Text="Distortion 3D View" VerticalAlignment="Center" FontWeight="Bold"/>
            <CheckBox Content="Show Surface" IsChecked="True" Checked="ShowSurface_Checked" Unchecked="ShowSurface_Unchecked"/>
            <CheckBox Content="Show Wire" IsChecked="True" Checked="ShowWire_Checked" Unchecked="ShowWire_Unchecked"/>
        </StackPanel>


        <!-- The custom control -->
        <!--<local:Distortion3DView x:Name="Dist3D" Grid.Row="1"
                    HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                    ZScale="0.25"
                    GridCols="64" GridRows="36"/>-->
    </Grid>
</UserControl>
